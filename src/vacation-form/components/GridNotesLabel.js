const GridNotesLabel = ({ text }) => {
  return (
    <div className="vacation-grid-box padding vacation-label-notes vacation-label">
      <p>{text}</p>
    </div>
  );
};

export default GridNotesLabel;
